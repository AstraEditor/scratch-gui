# pnpm is different from npm, 
# So we need let `pnpm install`is same as `npm install`.
# It's too troublesome to solve dependencies problems one by one.
shamefully-hoist=true
strict-peer-dependencies=false
auto-install-peers=false